#include "main.h"

/**
 * mul: Entry point
 * Description: multiplies two integers
 * @a: first number to multiply
 * @b: second number to multiply first number
 *
 * Return: Result of multiplication between a param and b param
 */
int mul(int a, int b)
{
	return (a * b);
}
