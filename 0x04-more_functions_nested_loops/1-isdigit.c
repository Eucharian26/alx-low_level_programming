#include "main.h"
#include <stdio.h>

/**
 * isdigit - Entry point
 * Description: checks for digit
 * @c: is an integer
 * Return: 1 if true. 0 if false.
 */
int _isdigit(int c)
{
	int c;

	int c = 0;

	for (c = '0'; c <= '9'; c++)
	{
		if (c <= 9)
		{
			return (1);
		}
	return (0);
}
